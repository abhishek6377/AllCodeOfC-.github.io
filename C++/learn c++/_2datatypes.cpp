#include<iostream>
using namespace std;
int main(){
cout<<"Data types defined which types of value use in variable that "
"is called data types"
"three types of data types" 
"1.basic ==> int float char boolean "
"2.derived(premitive)==> array function pointer"
"3.user-defined(non-premitive)==> structure class enum union"
"* variable means it is memory location where we store value "
"we also called it is container where we store values ";

int a=10;
cout<<"The value of a is "<<a;
return 0;
}
